# Frontend assets

Vite builds `resources/css/app.css` and `resources/js/app.js` into `public/build`.

JavaScript should progressively enhance server-rendered pages. Specialized runtimes and interactive engines belong in later milestones and must remain lazy-loaded.
